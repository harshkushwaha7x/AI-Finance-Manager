# Prisma Migration Scaffold

This directory is ready for the first migration once your database connection is configured.

## Recommended next step

Run:

```bash
npm run db:migrate -- --name init
```

That command will generate the first SQL migration in this folder and apply it to your database.
